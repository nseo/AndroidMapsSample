<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.example.android.maps"
      android:versionCode="1"
      android:versionName="1.0">
    <application android:icon="@drawable/icon" android:label="@string/app_name">
        <activity android:name=".Main"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Hello" android:label="@string/hello" />
        <activity android:name=".UseLayout" android:label="@string/use_layout" />
        <activity android:name=".Zoom" android:label="@string/zoom" />
        <activity android:name=".Settings" android:label="@string/settings" />
        <activity android:name=".ControllerSample" android:label="@string/controller" />
        <activity android:name=".Tracking" android:label="@string/tracking" />
        <activity android:name=".Landmarks" android:label="@string/landmarks" />
        <activity android:name=".OverlaySample" android:label="@string/overlay" />
        <uses-library android:name="com.google.android.maps" /><!-- here -->
    </application>
    <uses-permission android:name="android.permission.INTERNET" /><!-- here -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/><!-- here -->
    <uses-permission android:name="android.permission.ACCESS_MOCK_LOCATION"/><!-- here -->
</manifest>